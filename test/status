#!/bin/bash

echo ---------- 0 ----------
sqlite3 0/paxolite.db 'select * from log order by seq desc limit 2'
echo ---------- 1 ----------
sqlite3 1/paxolite.db 'select * from log order by seq desc limit 2'
echo ---------- 2 ----------
sqlite3 2/paxolite.db 'select * from log order by seq desc limit 2'
echo ---------- 3 ----------
sqlite3 3/paxolite.db 'select * from log order by seq desc limit 2'
echo ---------- 4 ----------
sqlite3 4/paxolite.db 'select * from log order by seq desc limit 2'
